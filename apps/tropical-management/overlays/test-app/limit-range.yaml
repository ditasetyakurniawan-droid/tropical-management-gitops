apiVersion: v1
kind: LimitRange
metadata:
  name: tropical-container-defaults
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 25m
        memory: 64Mi
      default:
        cpu: 500m
        memory: 256Mi
